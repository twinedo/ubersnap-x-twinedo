
// types
export type TFilterItem = {
    id: string;
    text: 'Default',
    effect: 'no-effect' | 'blur' | 'brighter',
    picture: any
}

export type TCropRect = { x: number; y: number; width: number; height: number }

export type TStartPoint = { x: number; y: number }


